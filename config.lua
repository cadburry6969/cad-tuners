-- Add the shop which should be shown in catelogue
catelogue_shop = "tuner" -- You need to have this in qbcore shared vehicles [shop]

-- Add the category here which you want to sell/spawn
category_vehice = "tuners" -- Category of the vehicle in qbcore shared vehicles [category]

-- Job which should have access for the menu and other stuff
tuner_job_name = "tuner"

-- Core export name
core_export = 'qb-core'

-- Fuel export name
fuel_export = 'LegacyFuel'

-- Menu export name
menu_export = 'qb-menu'

-- Target export name
target_export = 'qb-target'
